name: CD

on:
  push:
    tags:
      - "*.*.*"

permissions:
  contents: read
  packages: write

jobs:
  ci-tests:
    uses: ./.github/workflows/ci.yml

  build-and-push:
    needs: ci-tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v6
      with:
        context: .
        file: ./docker/Dockerfile_prod
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/microblog:${{ github.ref_name }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Cache pip
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
        cache: 'pip'
        cache-dependency-path: |
          requirements/deploy.txt
          ansible/requirements.yml

    - name: Force update ansible and pip packets for azure
      run: |
        ansible-galaxy collection install azure.azcollection --force
        pip install --upgrade msal msal-extensions azure-cli-core azure-common

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements/deploy.txt
        pip install -r ~/.ansible/collections/ansible_collections/azure/azcollection/requirements.txt
        cd ansible && ansible-galaxy install -r requirements.yml

    - name: Setup SSH 
      shell: bash
      run: |
        eval `ssh-agent -s`
        mkdir -p /home/runner/.ssh/
        touch /home/runner/.ssh/id_rsa
        echo -e "${{ secrets.SSH_PRIVATE_KEY }}" > /home/runner/.ssh/id_rsa
        chmod 700 /home/runner/.ssh/id_rsa

    - name: Run deploy playbook
      env:
        ANSIBLE_HOST_KEY_CHECKING: "False"
        ANSIBLE_CONFIG: "ansible.cfg"
      run: |
        cd ansible
        cat ansible.cfg
        ansible-playbook -i hosts gather_instances.yml deploy_app.yml \
          --private-key=/home/runner/.ssh/id_rsa \
          --extra-vars "image_tag=${{ github.ref_name }}"

